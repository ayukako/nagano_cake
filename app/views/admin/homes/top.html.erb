<h1>注文履歴一覧</h1>

<table>
  <tr>
    <th>購入日時</th>
    <th>購入者</th>
    <th>注文個数</th>
    <th>注文ステータス</th>
  </tr>
  <tr><% @order.each do |order| %>
  <th><%= order.created_at %></th>
  <th><%= order.customer.last_name%><%= order.customer.first_name %></th>
  <th><%= order.amount %></th>
  <th><%= order.status %></th>

  <% end %></tr>
</table>

