<h2>商品一覧</h2>

    <% @items.each do |item| %>

      <%= link_to public_item_path(item) do %>
        <%=  image_tag item.image, size: "150x150" %>
      <% end %>
    <td>
        <%= item.name %>
    </td>
    <td>
      <%= item.with_tax_price %>
    </td>
    <% end %>