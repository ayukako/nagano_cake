<h1>注文情報確認</h1>


<table>
  <thead>
      <th>商品名</th>
      <th>単価(税込)</th>
      <th>数量</th>
      <th>小計</th>
  </thead>
  <tbody>
  <% @cart_items.each do |cart_item| %>
      <tr>
        　<td><%= cart_item.item.name %></td>
          <td><%= cart_item.item.price %></td>
          <td><%= cart_item.item.price*cart_item.amount %></td>
  <% end %>
  </tbody>
</table>
<table>
  <thead>
      <th>送料</th>
      <th>商品合計</th>
      <th>請求額</th>
  </thead>
  <tbody>
</table>

<h4>支払い方法</h4>

<h4>お届け先</h4>

<%= link_to "注文を確定する", thanks_public_orders_path %>